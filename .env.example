# Generate a random value of at least 32 characters and export it before starting.
SESSION_SECRET=
PORT=3000
DB_PATH=data/pglite
# For free hosted deployment, set DATABASE_URL to your Neon PostgreSQL connection string.
# DATABASE_URL=
NODE_ENV=development
